# Runbooks

This folder will contain operational runbooks for routine and
emergency procedures (restarts, restores, recovery steps).

Naming convention: `runbook-service-action.md`,
e.g. `runbook-minecraft-restore.md`.

Sensitive or internal-only runbooks belong in `runbooks/private/`
(gitignored, not committed).

No runbooks exist yet.
