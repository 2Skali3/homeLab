# Audits

This folder will contain point-in-time audit reports (hardware, network,
security, storage) as they are performed.

Audit findings already gathered (2026-06-14) are currently consolidated
directly into `inventory/operations.md` and `inventory/infrastructure.md`
rather than as standalone files here. Standalone audit files will be
added here starting with the next formal audit cycle.
